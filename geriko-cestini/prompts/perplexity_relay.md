# Provider perplexity_relay — SLOT

La spec di questo provider è stata definita da Marco in un altro progetto.
Incollare qui: endpoint/metodo, query template per dominio, formato risposta, mapping verso i 4 flag, costi per chiamata.
Finché questo file non è compilato, il provider resta `enabled: false` in config.yaml e la pipeline usa solo scrape_direct.
